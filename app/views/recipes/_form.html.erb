<%= form_for(@recipe, html: {class: "form-horizontal"}) do |f| %>
  <% if @recipe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@recipe.errors.count, "error") %> prohibited this recipe from being saved:</h2>

      <ul>
      <% @recipe.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <div class="col-lg-3"><%= f.label :name %></div>
    <div class="col-lg-9"><%= f.text_field :name, :class => "form-control" %></div>
  </div>
  <div class="form-group">
    <div class="col-lg-3"><%= f.label :description %></div>
    <div class="col-lg-9"><%= f.text_area :description, :class => "form-control" %></div>
  </div>
  <div class="form-group">
    <div class="col-lg-3"><%= f.label :category %></div>
    <div class="col-lg-9"><%= f.text_field :category, :class => "form-control" %></div>
  </div>
  <div class="form-group">
    <div class="col-lg-3"></div>
    <div class="col-lg-9 text-right">
    <%= f.submit "Submit", :class => "btn btn-default" %> </div>
  </div>
<% end %>
